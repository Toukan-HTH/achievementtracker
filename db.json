{
    "achievements" : [{
        "generationFunction": "export function generate():number { return Math.floor(Math.random() * (10 - 1 + 1)) + 1; }",
        "validationFunction": "export function convertInternal(x:number):number {return x*2.54;}",
        "compareFunction": "export function compare():boolean { for (let index = 0; index < 5; index++) { let x = generate(); console.log(index); if(solve(x)!=convertInternal(x)){return false;}}return true;}",
        "difficultyRating":1,
        "description": "write a function that converts inches to centimeter",
        "id":0,
        "title":"Inches to centimeters",
        "creator":"Henrik T Hafsø",
        "completed":false,
        "signature": "export function solve(inches:number):number{}"
    }]
}